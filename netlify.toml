[build]
  command = "npm install -g pnpm && pnpm install && pnpm --filter @workspace/lop9a2 run build"
  publish = "artifacts/lop9a2/dist/public"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10"
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
